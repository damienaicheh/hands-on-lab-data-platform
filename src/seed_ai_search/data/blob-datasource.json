{
  "name": "${KS_NAME}-datasource",
  "description": "ADLS Gen2 data source (with POSIX ACL ingestion) for knowledge source '${KS_NAME}'",
  "type": "adlsgen2",
  "indexerPermissionOptions": ["userIds", "groupIds"],
  "credentials": {
    "connectionString": "${BLOB_DATASOURCE_CONNECTION_STRING}"
  },
  "container": {
    "name": "${BLOB_CONTAINER_NAME}"
  }
}